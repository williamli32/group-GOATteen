spring:
  application:
    name: trading-platform

  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5433/leap_trading
    username: postgres
    password: n3u3da!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: ZGV2ZWxvcG1lbnRfc2VjcmV0X2tleV9mb3JfdGVzdGluZ19wdXJwb3Nlc19vbmx5
    accessTokenExpirationMinutes: 60
  auth:
    refreshTokenExpirationDays: 7