spring:

  datasource:

    url: ${DB_URL:jdbc:postgresql://localhost:5432/leap_trading}

    username: ${DB_USERNAME:postgres}

    password: ${DB_PASSWORD:n3u3da!}
    
    driver-class-name: org.postgresql.Driver


  jpa:

    hibernate:
      ddl-auto: validate

    show-sql: true


  flyway:
    enabled: true